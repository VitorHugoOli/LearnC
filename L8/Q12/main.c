#include <stdio.h>
#include <stdlib.h>
struct imovel{
   char tipo[20],endereco[120],barrio[20],situacao[20];
   float valor;

}x;
int main()
{   int N,i;
    scanf("%d",&N)
    struct imovel x[N];
    fflush(stdin);
    gets(x.tipo);
    fflush(stdin);
    gets(x.endereco);
    fflush(stdin);
    gets(x.barrio);
    fflush(stdin);
    gets(x.situacao);
    scanf("%f",&valor);




   char tipo[20],situacao[20];
   float valor;
   gets(tipo)

    return 0;
}
