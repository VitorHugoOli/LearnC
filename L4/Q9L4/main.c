#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    float a,c,b;
    printf("digite a:");
    scanf("%f",&a);
    printf("digite c:");
    scanf("%f",&c);
    b=sqrt(a*c);
    printf("o termo medio dessa PG e:%f",b);

    return 0;
}
