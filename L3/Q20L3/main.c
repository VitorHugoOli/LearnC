#include <stdio.h>
#include <stdlib.h>

int main()
{
   int num, num_b;
   printf("digite seu intervalo:");
   scanf("%d, %d",&num, num_b);
   for(num=x;num_b<=y;num++);
   for(int count=x;count<=z;count++);

}


