#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,count;
    for(cont=1;cont<=20;cont++)
    {
        printf("digite o valor:");
        scanf("%d", &n);
        for
    }
    return 0;
}
